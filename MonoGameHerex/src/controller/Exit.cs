namespace MonoGameHerex
{
    // Represents the exit tile. As there is always only one exit tile at a time, it is static.
    public static class Exit
    {
        // This variable is used to determine whether or not the door is open or not and to render it accordingly.
        public static bool isOpen = false;
    }
}